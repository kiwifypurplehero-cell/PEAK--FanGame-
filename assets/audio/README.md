# Menu audio

Place licensed menu audio here. The `AudioController` is ready for hover, click, panel-open, panel-close, and music registrations; no placeholder audio is loaded, so missing files cannot cause requests or runtime failures.
