import {EngineManager} from './EngineManager.js';import {createCabin} from '../world/createCabin.js';import {PlayerController} from '../player/PlayerController.js';import {InteractionManager} from '../interactions/InteractionManager.js';import {WorkbenchUI} from '../ui/WorkbenchUI.js';
export class LobbyGame {
  constructor(canvas){this.canvas=canvas;this.core=new EngineManager(canvas)}
  async init(){const engine=this.core.create(),scene=new BABYLON.Scene(engine);scene.clearColor=BABYLON.Color4.FromHexString('#283b3dff');scene.collisionsEnabled=true;scene.gravity=new BABYLON.Vector3(0,-.18,0);const cabin=createCabin(scene);this.player=new PlayerController(scene,this.canvas);this.interactions=new InteractionManager(scene,this.player.camera,document.querySelector('#interaction-prompt'));this.ui=new WorkbenchUI(document.querySelector('#workbench-panel'),open=>{this.player.setEnabled(!open);this.interactions.enabled=!open;if(open)this.interactions.select(null)});this.interactions.register({mesh:cabin.workbenchTarget,interactableName:'Workbench',interactionText:'Use Workbench',interactionDistance:2.8,interactionKey:'e',interactionPoint:cabin.interactionPoint,interactionAction:()=>this.ui.show()});document.querySelector('#mobile-interact').onclick=()=>this.interactions.interact();scene.onBeforeRenderObservable.add(()=>{this.player.update();this.interactions.update();document.querySelector('#mobile-interact').classList.toggle('visible',!!this.interactions.current)});this.core.run(scene);await scene.whenReadyAsync();scene.render()}
  pause(){this.core.engine?.stopRenderLoop()}
  dispose(){this.ui?.dispose();this.interactions?.dispose();this.player?.dispose();this.core.dispose()}
}
